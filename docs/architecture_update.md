# ARCHITECTURE 文档同步说明

本次更新仅同步 `ARCHITECTURE.md` 与当前仓库实际目录，未调整任何目录命名、层级或脚本实现。

## 同步原则

- 以当前仓库根目录 `WM_prediction/src/` 为结构描述起点。
- 保留 `ARCHITECTURE.md` 既有的树状目录加行内注释格式。
- 仅记录版本库内当前可见的目录与关键脚本，不扩展为理想化结构。

## 本次反映的实际模块

- `conn_matrix/`：多数据集功能连接矩阵生成、Fisher Z 变换、向量化与批处理。
- `preprocess/`：数据预处理、头动筛选、协变量与样本列表生成。
- `prediction/`：基于 PLS 的年龄、认知和 p-factor 预测。
- `results_vis/`：Haufe 权重汇总与偏相关等结果分析。

## 未纳入的内容

- 运行期数据目录、集群输出目录和仓库外部数据路径未写入结构树。
- `.git/` 等版本控制内部目录未作为项目架构的一部分说明。
